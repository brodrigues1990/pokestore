import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            light: '#FFF',
            main: '#005BAC',
            dark: '#FFF',
            contrastText: '#FFF',
          },
    },
  });

  export default theme;